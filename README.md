symfony2.8-with-capistrano
==========================

## Capistrano

    $ make init
    bundle init
    bundle add capistrano-symfony --version="~> 1.0.0.rc1"
    bundle install
    bundle exec cap install
